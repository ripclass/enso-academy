# AGENTS.md

This project uses `CLAUDE.md` as the canonical AI-assistant memory file. Regardless of which AI coding tool you are using (Claude Code, Cursor, Cline, Continue, Windsurf, etc.), read these files in order before doing any work:

1. `CLAUDE.md` — current project state, active priorities, conventions
2. `docs/ARCHITECTURE.md` — canonical design, do not deviate
3. `docs/PROGRESS.md` — milestone log
4. `docs/SESSION-NOTES.md` — running notes between sessions
5. `docs/decisions/` — Architecture Decision Records (ADRs) for significant choices
6. `docs/COURSE-GENERATION-PROMPT.md` — only when working on course content generation

Then update those files at the end of every meaningful work block. The memory system is non-negotiable. See CLAUDE.md "Memory system discipline" section.

Do not duplicate context into this file. CLAUDE.md is the source of truth.
